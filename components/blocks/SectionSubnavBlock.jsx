import TravelSubnav from '../chrome/TravelSubnav.jsx';

/** The travel sub-navigation. No fields; the links live in TravelSubnav until W1.11. */
export default function SectionSubnavBlock({ locale }) {
  return <TravelSubnav locale={locale} />;
}
