node_modules/
.next/
out/
build/

# env / secrets
.env
.env*.local

# uploads written by the admin panel at runtime
public/uploads/
# local fallback for MEDIA_ROOT (lib/media.js uploadRoot()) — outside public/
# on purpose, served by app/uploads/[...path]/route.js instead
var/uploads/

# release tarballs written by scripts/package-standalone.mjs --tar
var/dist/
# git worktree used by scripts/release-to-branch.mjs
var/release-worktree/

# local scratch / screenshots
.playwright-mcp/
*.log
local-*.png
live-*.png
nav-fixed.png
hero-restored.png

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# test artifacts
test-results/
playwright-report/
coverage/

# local visual-check scratch (screenshots + throwaway scripts)
var/shots/
var/hyd.mjs
var/rate.mjs
