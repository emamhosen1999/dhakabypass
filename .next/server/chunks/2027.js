"use strict";exports.id=2027,exports.ids=[2027],exports.modules={72027:(e,t,s)=>{s.d(t,{assertBootEnvironment:()=>g});var i=s(51455),a=s.n(i),n=s(76760),o=s.n(n);let r=new Set(["localhost","127.0.0.1","0.0.0.0","::1","[::1]"]);function l(e){let t=String(null==e?"":e).trim();if(!t)return null;let s=/^https?:\/\//i.test(t)?t:`https://${t}`;try{return new URL(s).origin}catch{return null}}function h(e){return null==e||""===String(e).trim()}async function d(e,t){let i;let a=t||await Promise.resolve().then(s.t.bind(s,51455,23)),n=(e,t,s)=>[{key:"MEDIA_ROOT",message:e,why:t,fix:s}];try{i=await a.stat(e)}catch{return n(`MEDIA_ROOT does not exist: ${e}`,"The directory is created on demand by the first upload, so a typo becomes a real directory in the wrong place and nothing ever reports it.",`Create it before starting the app:  mkdir -p ${e} && chmod 750 ${e}`)}if(!i.isDirectory())return n(`MEDIA_ROOT is not a directory: ${e}`,"Uploads cannot be written under a path that is a file.","Point MEDIA_ROOT at a directory.");try{await a.access(e,2)}catch{return n(`MEDIA_ROOT is not writable by this user: ${e}`,"Uploads would fail at the moment a content editor tries to publish, not now.",`Give the application user write permission:  chmod 750 ${e}`)}return[]}function c({problems:e=[],warnings:t=[]}={},{heading:s,cwd:i}={}){let a="=".repeat(76),n=[],o=(e,t)=>{n.push(`  ${e} ${t.message}`),t.why&&n.push(...u(t.why,"      why: ","           ")),t.fix&&n.push(...u(t.fix,"      fix: ","           ")),n.push("")};if(n.push(""),n.push(a),n.push(`  ${s||"DHAKA BYPASS — environment check"}`),i&&n.push(`  release directory: ${i}`),n.push(a),n.push(""),e.length)for(let t of(n.push(`  ${e.length} problem${1===e.length?"":"s"} — the app must not serve production like this:`),n.push(""),e))o("x",t);if(t.length)for(let e of(n.push(`  ${t.length} warning${1===t.length?"":"s"} — the site will serve, but something is wrong:`),n.push(""),t))o("!",e);return e.length||t.length||(n.push("  All checks passed."),n.push("")),n.push(a),n.push(""),n.join("\n")}function u(e,t,s,i=74){let a=String(e).split(/\s+/),n=[],o=t,r=t;for(let e of a)o.length+e.length+1>i&&o!==r?(n.push(o),r=s,o=s+e):o=o===r?o+e:`${o} ${e}`;return o.trim()&&n.push(o),n}async function p(e){try{return JSON.parse(await a().readFile(o().join(e,"build-info.json"),"utf8"))}catch{return null}}async function g({env:e=process.env,cwd:t=process.cwd(),stderr:s=process.stderr,exit:i=e=>process.exit(e)}={}){if(!function(e={}){return"production"===e.NODE_ENV&&(!e.NEXT_PHASE||"phase-production-server"===e.NEXT_PHASE)}(e))return{checked:!1};let a=await p(t),n=function({env:e={},cwd:t=process.cwd(),buildInfo:s=null}={}){let i=[],a=[],n=(e,t,s,a)=>i.push({key:e,message:t,why:s,fix:a}),d=(e,t,s,i)=>a.push({key:e,message:t,why:s,fix:i}),c="git-pull"===(s&&s.deployModel||"replace"),u=e.MEDIA_ROOT,p=o().join(t,"var","uploads");h(u)?c?d("MEDIA_ROOT",`MEDIA_ROOT is not set — uploads will go to ${p}`,"This release is deployed by updating a git checkout in place, and a pull does not delete untracked files, so uploads there survive a deploy. Two things still bite: a fresh clone into a new directory starts with an empty media library while the database still references every file, and a host backup that only covers the database will not carry the images.","Fine to leave. To make it robust, set MEDIA_ROOT to a path outside the checkout that the account backup covers, and move the existing files there."):n("MEDIA_ROOT","MEDIA_ROOT is not set.",`Uploads would fall back to ${p} — inside the release directory. This release is deployed by replacing that directory, so every image uploaded through the admin would be deleted by the next deploy, with no error at upload time and no trace afterwards.`,"Set MEDIA_ROOT to an absolute path OUTSIDE the release directory, e.g. /home/aeos365/media/dhakabypass, and create the directory before starting the app."):o().isAbsolute(String(u))?function(e,t){let s=o().resolve(e),i=o().resolve(t);return s===i||i.startsWith(s.endsWith(o().sep)?s:s+o().sep)}(t,String(u))&&(c?d("MEDIA_ROOT",`MEDIA_ROOT is inside the release directory: ${u}`,"Deploys update this checkout in place and leave untracked files alone, so this survives — provided the path stays untracked and git-ignored. It does not survive a fresh clone into a new directory.","Acceptable as it stands. A path outside the checkout is more robust."):n("MEDIA_ROOT",`MEDIA_ROOT is inside the release directory: ${u}`,`The release directory (${t}) is replaced wholesale on every deploy. Anything written under it is lost at the next release.`,"Move MEDIA_ROOT outside the release directory — a sibling such as /home/aeos365/media/dhakabypass, which the deploy never touches and the account backup does.")):n("MEDIA_ROOT",`MEDIA_ROOT is not an absolute path: ${u}`,"A relative path is resolved against the process working directory, which Passenger sets to the release directory — so a relative value is not the path you think you set, and it moves if anything ever changes the working directory.","Use a full path beginning with /.");let g=e.SITE_URL,S=l(g);if(h(g))n("SITE_URL","SITE_URL is not set.","lib/seo/site.js defaults to http://localhost:3000. robots.txt would advertise a localhost sitemap, and every canonical and hreflang alternate would point at localhost. Nothing errors; the site quietly deindexes itself.","Set SITE_URL to this deployment's own absolute origin, e.g. https://staging.dhakabypass.com on staging and https://dhakabypass.com in production.");else if(S){let e=new URL(S).hostname;r.has(e)&&n("SITE_URL",`SITE_URL points at a local address: ${S}`,"Canonicals, hreflang alternates and the sitemap would all publish an address no crawler and no visitor can reach.","Set SITE_URL to the public origin this deployment answers on."),S.startsWith("http://")&&d("SITE_URL",`SITE_URL uses http, not https: ${S}`,"Canonical URLs would nominate the insecure variant of every page as the one to index, and the site is served over TLS.","Use https:// unless this host genuinely has no certificate."),/^https?:\/\//i.test(String(g).trim())||d("SITE_URL",`SITE_URL has no scheme and was read as ${S}`,"lib/seo/site.js accepts a bare hostname and assumes https. That is deliberate, but it means the value in the environment editor does not match what gets published.",`Write it out in full: ${S}`)}else n("SITE_URL",`SITE_URL cannot be parsed as a URL: ${g}`,"lib/seo/site.js silently falls back to http://localhost:3000 when the value is unparseable, so this fails exactly like an unset value.","Use a full origin such as https://dhakabypass.com — no path, no trailing slash.");let y=s&&s.siteUrl;if(!h(y)&&S){let e=l(y);e&&e!==S&&n("SITE_URL",`SITE_URL (${S}) does not match the origin this artifact was built with (${e}).`,"The localised pages and the sitemap were prerendered at build time and carry the build origin in their canonical and hreflang tags. Serving this artifact from a different origin publishes the wrong URLs until every page revalidates.",`Either serve this artifact from ${e}, or rebuild with SITE_URL=${S}. An artifact belongs to one origin — do not promote the staging tarball to production.`)}let A=["DB_HOST","DB_NAME","DB_USER"].filter(t=>h(e[t]));A.length?n("DB",`Database configuration is incomplete — not set: ${A.join(", ")}.`,'lib/db.js treats an incomplete configuration as "no database" and every reader degrades to safe defaults, so the site would answer every request with HTTP 200 and empty content rather than reporting a fault.',"Set DB_HOST, DB_PORT, DB_NAME, DB_USER and DB_PASSWORD in the app environment."):h(e.DB_PASSWORD)&&d("DB_PASSWORD","DB_PASSWORD is empty.","Valid only if the database user genuinely has no password, which no cPanel MySQL user does. If it is a mistake, every query fails and the site renders empty.","Set DB_PASSWORD to the password of the cPanel database user.");let f=String(e.ANALYTICS_PROVIDER||"").trim().toLowerCase();if(f&&"none"!==f){let t=["plausible","umami","ga4"],s=String(e.ANALYTICS_SITE_ID||"").trim(),i=String(e.ANALYTICS_SCRIPT_URL||"").trim();t.includes(f)?s?"plausible"!==f&&"umami"!==f||i||d("ANALYTICS_SCRIPT_URL",`ANALYTICS_PROVIDER is "${f}" but ANALYTICS_SCRIPT_URL is not set.`,"Both are normally self-hosted and each instance has its own script URL; there is no default to fall back to, so nothing is rendered.","Point ANALYTICS_SCRIPT_URL at the script on your analytics instance."):d("ANALYTICS_SITE_ID",`ANALYTICS_PROVIDER is "${f}" but ANALYTICS_SITE_ID is not set.`,"No analytics script is rendered, so the site looks instrumented and records nothing.","Set ANALYTICS_SITE_ID, or set ANALYTICS_PROVIDER=none."):d("ANALYTICS_PROVIDER",`ANALYTICS_PROVIDER is "${f}", which is not recognised.`,`Nothing will be measured. Known providers: ${t.join(", ")}.`,"Set it to one of those, or to none.")}return h(e.AUTH_SECRET)&&d("AUTH_SECRET","AUTH_SECRET is not set.","NextAuth reads this directly — no file in this repo does — so nothing fails at boot. Every /admin route and /api/auth route will return 500 on first use. The public site is unaffected, which is why this is a warning and not a refusal.","Generate one with: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""),h(e.ADMIN_EMAILS)&&d("ADMIN_EMAILS","ADMIN_EMAILS is empty.","auth.js fails closed on an empty list by design, so no account can sign in to the admin at all. The public site is unaffected.","Set ADMIN_EMAILS to a comma-separated list of the addresses allowed to administer the site."),{problems:i,warnings:a}}({env:e,cwd:t,buildInfo:a});if(!n.problems.some(e=>"MEDIA_ROOT"===e.key)&&e.MEDIA_ROOT&&n.problems.push(...await d(e.MEDIA_ROOT)),!n.problems.length)return n.warnings.length&&s.write(c(n,{heading:"DHAKA BYPASS — starting, with warnings",cwd:t})),{checked:!0,started:!0,result:n};let u="i-accept-the-risk"===e.DHAKABYPASS_SKIP_ENV_CHECK;return(s.write(c(n,{heading:u?"DHAKA BYPASS — environment check OVERRIDDEN, starting anyway":"DHAKA BYPASS — REFUSING TO START",cwd:t})),u)?(s.write("  DHAKABYPASS_SKIP_ENV_CHECK is set. The problems above are NOT fixed — the app\n  is serving with them. Unset it as soon as the real cause is dealt with.\n\n"),{checked:!0,started:!0,overridden:!0,result:n}):(s.write("  Fix the problems above and restart (touch tmp/restart.txt).\n  To check without starting the app:  node preflight.mjs\n  Full procedure: docs/deployment/2026-09-04-deploy-runbook.md\n\n"),i(1),{checked:!0,started:!1,result:n})}}};