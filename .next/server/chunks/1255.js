"use strict";exports.id=1255,exports.ids=[1255],exports.modules={41255:(a,b,c)=>{c.d(b,{assertBootEnvironment:()=>p});var d=c(51455),e=c.n(d),f=c(76760),g=c.n(f);let h=new Set(["localhost","127.0.0.1","0.0.0.0","::1","[::1]"]);function i(a){let b=String(null==a?"":a).trim();if(!b)return null;let c=/^https?:\/\//i.test(b)?b:`https://${b}`;try{return new URL(c).origin}catch{return null}}function j(a){return null==a||""===String(a).trim()}async function k(a,b){let d,e=b||await Promise.resolve().then(c.t.bind(c,51455,23)),f=(a,b,c)=>[{key:"MEDIA_ROOT",message:a,why:b,fix:c}];try{d=await e.stat(a)}catch{return f(`MEDIA_ROOT does not exist: ${a}`,"The directory is created on demand by the first upload, so a typo becomes a real directory in the wrong place and nothing ever reports it.",`Create it before starting the app:  mkdir -p ${a} && chmod 750 ${a}`)}if(!d.isDirectory())return f(`MEDIA_ROOT is not a directory: ${a}`,"Uploads cannot be written under a path that is a file.","Point MEDIA_ROOT at a directory.");try{await e.access(a,2)}catch{return f(`MEDIA_ROOT is not writable by this user: ${a}`,"Uploads would fail at the moment a content editor tries to publish, not now.",`Give the application user write permission:  chmod 750 ${a}`)}return[]}function l({problems:a=[],warnings:b=[]}={},{heading:c,cwd:d}={}){let e="=".repeat(76),f=[],g=(a,b)=>{f.push(`  ${a} ${b.message}`),b.why&&f.push(...m(b.why,"      why: ","           ")),b.fix&&f.push(...m(b.fix,"      fix: ","           ")),f.push("")};if(f.push(""),f.push(e),f.push(`  ${c||"DHAKA BYPASS — environment check"}`),d&&f.push(`  release directory: ${d}`),f.push(e),f.push(""),a.length)for(let b of(f.push(`  ${a.length} problem${1===a.length?"":"s"} — the app must not serve production like this:`),f.push(""),a))g("x",b);if(b.length)for(let a of(f.push(`  ${b.length} warning${1===b.length?"":"s"} — the site will serve, but something is wrong:`),f.push(""),b))g("!",a);return a.length||b.length||(f.push("  All checks passed."),f.push("")),f.push(e),f.push(""),f.join("\n")}function m(a,b,c,d=74){let e=String(a).split(/\s+/),f=[],g=b,h=b;for(let a of e)g.length+a.length+1>d&&g!==h?(f.push(g),h=c,g=c+a):g=g===h?g+a:`${g} ${a}`;return g.trim()&&f.push(g),f}async function n(a){let b,{missingMigrations:d,migrationProblem:e,readAppliedMigrations:f}=await c.e(4320).then(c.bind(c,24320));if(a)b=await a();else{let{query:a,dbEnabled:d}=await c.e(2124).then(c.bind(c,72124));if(!d())return null;b=await f(a)}return e(d(b.applied),b.error)}async function o(a){try{return JSON.parse(await e().readFile(g().join(a,"build-info.json"),"utf8"))}catch{return null}}async function p({env:a=process.env,cwd:b=process.cwd(),stderr:c=process.stderr,exit:d=a=>process.exit(a),readMigrations:e=null}={}){if(!function(a={}){return"production"===a.NODE_ENV&&(!a.NEXT_PHASE||"phase-production-server"===a.NEXT_PHASE)}(a))return{checked:!1};let f=await o(b),m=function({env:a={},cwd:b=process.cwd(),buildInfo:c=null}={}){let d=[],e=[],f=(a,b,c,e)=>d.push({key:a,message:b,why:c,fix:e}),k=(a,b,c,d)=>e.push({key:a,message:b,why:c,fix:d}),l="git-pull"===(c&&c.deployModel||"replace"),m=a.MEDIA_ROOT,n=g().join(b,"var","uploads");j(m)?l?k("MEDIA_ROOT",`MEDIA_ROOT is not set — uploads will go to ${n}`,"This release is deployed by updating a git checkout in place, and a pull does not delete untracked files, so uploads there survive a deploy. Two things still bite: a fresh clone into a new directory starts with an empty media library while the database still references every file, and a host backup that only covers the database will not carry the images.","Fine to leave. To make it robust, set MEDIA_ROOT to a path outside the checkout that the account backup covers, and move the existing files there."):f("MEDIA_ROOT","MEDIA_ROOT is not set.",`Uploads would fall back to ${n} — inside the release directory. This release is deployed by replacing that directory, so every image uploaded through the admin would be deleted by the next deploy, with no error at upload time and no trace afterwards.`,"Set MEDIA_ROOT to an absolute path OUTSIDE the release directory, e.g. /home/aeos365/media/dhakabypass, and create the directory before starting the app."):g().isAbsolute(String(m))?function(a,b){let c=g().resolve(a),d=g().resolve(b);return c===d||d.startsWith(c.endsWith(g().sep)?c:c+g().sep)}(b,String(m))&&(l?k("MEDIA_ROOT",`MEDIA_ROOT is inside the release directory: ${m}`,"Deploys update this checkout in place and leave untracked files alone, so this survives — provided the path stays untracked and git-ignored. It does not survive a fresh clone into a new directory.","Acceptable as it stands. A path outside the checkout is more robust."):f("MEDIA_ROOT",`MEDIA_ROOT is inside the release directory: ${m}`,`The release directory (${b}) is replaced wholesale on every deploy. Anything written under it is lost at the next release.`,"Move MEDIA_ROOT outside the release directory — a sibling such as /home/aeos365/media/dhakabypass, which the deploy never touches and the account backup does.")):f("MEDIA_ROOT",`MEDIA_ROOT is not an absolute path: ${m}`,"A relative path is resolved against the process working directory, which Passenger sets to the release directory — so a relative value is not the path you think you set, and it moves if anything ever changes the working directory.","Use a full path beginning with /.");let o=a.SITE_URL,p=i(o);if(j(o))f("SITE_URL","SITE_URL is not set.","lib/seo/site.js defaults to http://localhost:3000. robots.txt would advertise a localhost sitemap, and every canonical and hreflang alternate would point at localhost. Nothing errors; the site quietly deindexes itself.","Set SITE_URL to this deployment's own absolute origin, e.g. https://staging.dhakabypass.com on staging and https://dhakabypass.com in production.");else if(p){let a=new URL(p).hostname;h.has(a)&&f("SITE_URL",`SITE_URL points at a local address: ${p}`,"Canonicals, hreflang alternates and the sitemap would all publish an address no crawler and no visitor can reach.","Set SITE_URL to the public origin this deployment answers on."),p.startsWith("http://")&&k("SITE_URL",`SITE_URL uses http, not https: ${p}`,"Canonical URLs would nominate the insecure variant of every page as the one to index, and the site is served over TLS.","Use https:// unless this host genuinely has no certificate."),/^https?:\/\//i.test(String(o).trim())||k("SITE_URL",`SITE_URL has no scheme and was read as ${p}`,"lib/seo/site.js accepts a bare hostname and assumes https. That is deliberate, but it means the value in the environment editor does not match what gets published.",`Write it out in full: ${p}`)}else f("SITE_URL",`SITE_URL cannot be parsed as a URL: ${o}`,"lib/seo/site.js silently falls back to http://localhost:3000 when the value is unparseable, so this fails exactly like an unset value.","Use a full origin such as https://dhakabypass.com — no path, no trailing slash.");let q=c&&c.siteUrl;if(!j(q)&&p){let a=i(q);a&&a!==p&&f("SITE_URL",`SITE_URL (${p}) does not match the origin this artifact was built with (${a}).`,"The localised pages and the sitemap were prerendered at build time and carry the build origin in their canonical and hreflang tags. Serving this artifact from a different origin publishes the wrong URLs until every page revalidates.",`Either serve this artifact from ${a}, or rebuild with SITE_URL=${p}. An artifact belongs to one origin — do not promote the staging tarball to production.`)}let r=["DB_HOST","DB_NAME","DB_USER"].filter(b=>j(a[b]));r.length?f("DB",`Database configuration is incomplete — not set: ${r.join(", ")}.`,'lib/db.js treats an incomplete configuration as "no database" and every reader degrades to safe defaults, so the site would answer every request with HTTP 200 and empty content rather than reporting a fault.',"Set DB_HOST, DB_PORT, DB_NAME, DB_USER and DB_PASSWORD in the app environment."):j(a.DB_PASSWORD)&&k("DB_PASSWORD","DB_PASSWORD is empty.","Valid only if the database user genuinely has no password, which no cPanel MySQL user does. If it is a mistake, every query fails and the site renders empty.","Set DB_PASSWORD to the password of the cPanel database user.");let s=String(a.ANALYTICS_PROVIDER||"").trim().toLowerCase();if(s&&"none"!==s){let b=["plausible","umami","ga4"],c=String(a.ANALYTICS_SITE_ID||"").trim(),d=String(a.ANALYTICS_SCRIPT_URL||"").trim();b.includes(s)?c?"plausible"!==s&&"umami"!==s||d||k("ANALYTICS_SCRIPT_URL",`ANALYTICS_PROVIDER is "${s}" but ANALYTICS_SCRIPT_URL is not set.`,"Both are normally self-hosted and each instance has its own script URL; there is no default to fall back to, so nothing is rendered.","Point ANALYTICS_SCRIPT_URL at the script on your analytics instance."):k("ANALYTICS_SITE_ID",`ANALYTICS_PROVIDER is "${s}" but ANALYTICS_SITE_ID is not set.`,"No analytics script is rendered, so the site looks instrumented and records nothing.","Set ANALYTICS_SITE_ID, or set ANALYTICS_PROVIDER=none."):k("ANALYTICS_PROVIDER",`ANALYTICS_PROVIDER is "${s}", which is not recognised.`,`Nothing will be measured. Known providers: ${b.join(", ")}.`,"Set it to one of those, or to none.")}return j(a.AUTH_SECRET)&&k("AUTH_SECRET","AUTH_SECRET is not set.","NextAuth reads this directly — no file in this repo does — so nothing fails at boot. Every /admin route and /api/auth route will return 500 on first use. The public site is unaffected, which is why this is a warning and not a refusal.","Generate one with: node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""),j(a.ADMIN_EMAILS)&&k("ADMIN_EMAILS","ADMIN_EMAILS is empty.","auth.js fails closed on an empty list by design, so no account can sign in to the admin at all. The public site is unaffected.","Set ADMIN_EMAILS to a comma-separated list of the addresses allowed to administer the site."),{problems:d,warnings:e}}({env:a,cwd:b,buildInfo:f});if(!m.problems.some(a=>"MEDIA_ROOT"===a.key)&&a.MEDIA_ROOT&&m.problems.push(...await k(a.MEDIA_ROOT)),!m.problems.some(a=>String(a.key).startsWith("DB"))){let a=await n(e);a&&m.problems.push(a)}if(!m.problems.length)return m.warnings.length&&c.write(l(m,{heading:"DHAKA BYPASS — starting, with warnings",cwd:b})),{checked:!0,started:!0,result:m};let q="i-accept-the-risk"===a.DHAKABYPASS_SKIP_ENV_CHECK;return(c.write(l(m,{heading:q?"DHAKA BYPASS — environment check OVERRIDDEN, starting anyway":"DHAKA BYPASS — REFUSING TO START",cwd:b})),q)?(c.write("  DHAKABYPASS_SKIP_ENV_CHECK is set. The problems above are NOT fixed — the app\n  is serving with them. Unset it as soon as the real cause is dealt with.\n\n"),{checked:!0,started:!0,overridden:!0,result:m}):(c.write("  Fix the problems above and restart (touch tmp/restart.txt).\n  To check without starting the app:  node preflight.mjs\n  Full procedure: docs/deployment/2026-09-04-deploy-runbook.md\n\n"),d(1),{checked:!0,started:!1,result:m})}}};