# CMS consistency audit — 2026-09-12

Scope: the public tree (`app/[locale]/**`), the chrome, the 41 block types and their renderers, the corridor/contact/request/newsletter components, `lib/i18n/ui.js`, and the admin screens that are supposed to edit all of it. Read-only; every finding below was verified by reading the code, and the "live" claims were verified against the local development database (`site_settings`, `pages`, `blocks`, `block_translations`, `menus`, `ui_strings`, `toll_rates`, `toll_od_rates`, `interchanges`, `segments`).

Governing requirement (client): *"100% brutal, no hardcoding, only block editor with preview of the blocks, and edit of block, full CMS managed."* Facts live once, in records; blocks carry presentation; every visible string traces to a block field, a record, a `ui_string`, or a menu. The three declared code exceptions are `[[...slug]]`, `/news/[slug]` and `/preview/[id]`.

## Summary

1. **Hardcoded public text** — 11 findings (0 blocker, 3 major, 8 minor). The header theme toggle is untranslatable English on every locale; block documents that fall back to English carry no notice and lose their Bangla/Chinese chrome; the rest are aria/tooltip/unit literals.
2. **Hardcoded facts/numbers** — 15 findings (5 blocker, 2 major, 8 minor). `999` is in code twice; the default `<title>`/description in `lib/seo/settings.js` carry a superlative claim and "48km" and are LIVE because `seo.site_title`/`seo.site_description` are `{}` in the database; highway names and RHD links are a code table; the map's accessible name hardcodes "Naojor to Madanpur".
3. **Admin ↔ public consistency** — 9 findings (0 blocker, 3 major, 4 minor, 2 verified-consistent). `card-grid` shows an image picker the renderer ignores; four social-link settings are saved and never rendered; otherwise all 41 types' fields are rendered and no renderer reads an undeclared key; the preview provably shares the renderer and presentation classes.
4. **Operator dead ends** — 16 findings (0 blocker, 11 major, 5 minor). Two settings (`corridor.prohibited_vehicles`, `corridor.published_length_km`) have no admin screen; footer legal links and the header Contact CTA are code targets outside the menus; 31 keys on `/admin/translations` have no call site; unpublishing an "essential" page leaves it in the sitemap.
5. **Records vs blocks** — 14 findings (6 blocker, 5 major, 2 minor, 1 verified-consistent). At type level, `toll-table` authors gazette provenance per block while `toll_od_rates` holds it per record, and `stat-row`/`data-table` have no record-bound mode. At content level the live database already holds a retyped toll schedule (block 425) and retyped interchange chainages that CONTRADICT the `interchanges` records on the same page (blocks 422, 424), plus the corridor length typed into six blocks.
6. **Tests** — 10 existing drift guards noted, 10 gaps listed; none of the blockers above is caught by a test today, and two existing tests lock the current behaviour in (`seo-settings` asserts the "48km" default; `seo-sitemap` asserts essential paths are always emitted).

---

## 1. Hardcoded public text

Ignored as instructed: developer comments, CSS classes, admin screens, digit/unit formatting helpers. Error boundaries are listed for visibility only.

| # | file:line | finding | severity | suggested fix |
|---|---|---|---|---|
| 1.1 | `components/chrome/ThemeToggle.jsx:6,34` | `const LABELS = { light: 'Light', dark: 'Dark', system: 'System' }` rendered as the three visible button labels in the header on `/en`, `/bn` and `/zh`. Only the group's `aria-label` comes from `t(locale,'theme')`. No `ui_string` exists for the three words, so an operator cannot translate the header. | major | Add `themeLight`/`themeDark`/`themeSystem` to `lib/i18n/ui.js` (all three locales); resolve them in `SiteHeaderV2.jsx` and pass as a `labels` prop, as `LocaleSwitch`/`TravelSubnav` already receive theirs. |
| 1.2 | `components/blocks/BlockRenderer.jsx:60` (`locale={resolved.locale}`) with `lib/content/resolve.js:17` | When a block has no published `bn`/`zh` row, the English data is rendered AND the component receives `locale='en'`, so every `t()` chrome inside the block (table headers, "Not measured", captions, legend) also flips to English although Bangla/Chinese `ui_strings` exist. `resolveTranslation` returns `fallback: true` and the renderer discards it: no "shown in English" notice, unlike `NewsListBlock.jsx:53` and `news/[slug]/page.jsx:118`. The local DB currently has 17 block groups on `/bn` in this state (`project`, `about`, `about/governance`, `sustainability`, `travel/route`, `travel/facilities`, `travel/status`, `travel/toll`). | major | Pass the PAGE locale to the component and the data from the fallback row; render a `db-newsfallback`-style notice from `newsInEnglish` (or a new `blockInEnglish` key) when `resolved.fallback` is true. |
| 1.3 | `components/chrome/SiteHeaderV2.jsx:72,88` | `aria-label="Primary"` and `aria-label="Primary, compact"` — accessible names of the two navs are English literals; every other aria-label in the chrome goes through `t()`. | minor | `t(locale,'navPrimary')` / `t(locale,'navPrimaryCompact')`. |
| 1.4 | `components/corridor/CorridorMap.jsx:29` | SVG `<title>` fallback `'Connecting road'` / `'Crossing road'` (hover tooltip on the approach roads) is an English literal although `lib/i18n/map-ui.js` already has `connected`/`crossing` in three locales. | minor | Pass `ui.connected`/`ui.crossing` into `CorridorMap` (add a `ui` prop). |
| 1.5 | `components/corridor/ProgressBar.jsx:39` | `{formatKm(...)} km / {totalKm} km` — two literal `km` while the map, toll matrix and calculator use `t(locale,'mapKm')`; on `/bn` the calculator says "কিমি" and the progress bar says "km". | minor | `t(locale,'mapKm')`. |
| 1.6 | `components/blocks/CorridorMapBlock.jsx:112` | `mUnit: locale === 'bn' ? 'মি' : locale === 'zh' ? '米' : 'm'` — an inline three-locale table in a component; not editable at `/admin/translations`. | minor | Add `mapM` to `ui.js` beside `mapKm`. |
| 1.7 | `components/blocks/PullQuoteBlock.jsx:27` | `<a className="db-pullquote-source" href={sourceHref}>↗</a>` — the source link's only content is an arrow glyph: no accessible name, no editable text. `CorridorExplorer.jsx:287` does it right (`{ui.roadSource} ↗`). | minor | Render `t(locale,'quoteSource')` (new key) with the arrow `aria-hidden`. |
| 1.8 | `lib/blocks/sitemapList.js:49,68-70` (`humanise`) | A sitemap-list group whose section has no parent page and no `nav*` key is headed by the slug with dashes replaced (`land acquisition`), English on every locale. | minor | Fall back to the first child page's title or hide the heading; never humanise a slug on a public page. |
| 1.9 | `app/[locale]/preview/[id]/page.jsx:32` | `title: 'Draft preview'` — staff-only route, session-gated, `noindex`. Reported for visibility; acceptable. | minor | none required. |
| 1.10 | `app/[locale]/error.jsx`, `app/global-error.jsx` | Error boundaries are code by design; every visible string is `t(locale,'error*')`. `global-error` renders from the code table only (no `UiStringsBridge`), which is documented in `ui.js:163-168`. | minor | none required; keep the `error*` keys translated in code. |
| 1.11 | `app/[locale]/not-found.jsx:36-43` | The fallback 404 (when the `not-found` row is missing/unpublished) is code, but every string is `t()`. Acceptable. | minor | none required. |

Verified clean (no literal visible text): `app/[locale]/layout.jsx`, `[[...slug]]/page.jsx`, `news/[slug]/page.jsx`, `SiteFooterV2.jsx` (except 2.1), `LocaleSwitch.jsx`, `ConsentBanner.jsx`, `TravelSubnav.jsx`, `AdvisoryBar.jsx`, `CorridorStrip.jsx`, `CorridorExplorer.jsx`, `ContactForm.jsx`, `RequestForm.jsx`, `NewsletterSignup.jsx`, and all 41 `components/blocks/*Block.jsx` (the em-dash placeholders in `TollCalculatorBlock.jsx:154`, `TollMatrixBlock.jsx:139`, `InterchangeTableBlock.jsx:51`, `TrafficStatusBlock.jsx:133` are punctuation, not copy).

## 2. Hardcoded facts / numbers

| # | file:line | finding | severity | suggested fix |
|---|---|---|---|---|
| 2.1 | `components/chrome/SiteFooterV2.jsx:102` | `<a className="db-footer-emergency-number" href="tel:999">999</a>` — the national emergency number is a code literal on every page of the site, beside the DBEDC number that IS a setting (`contact.emergency_phone`). | blocker | New setting `contact.national_emergency_phone` on `/admin/settings` (Contact); render only when non-blank, exactly as the DBEDC number is handled. |
| 2.2 | `components/blocks/EmergencyStripBlock.jsx:33` | Second copy of `href="tel:999">999` in the block. Two places to change one number. | blocker | Same setting as 2.1; one helper used by both. |
| 2.3 | `lib/seo/settings.js:67` | `siteTitle: "Dhaka Bypass Expressway - Bangladesh's First Fully Access-Controlled Highway"` — an unverified superlative as the default `<title>`. **Live:** `site_settings.seo.site_title` is `{}` in the DB, so this is the `<title>` of the 404 document and of any route whose `generateMetadata` returns `{}` (`[[...slug]]/page.jsx:88-90` for a missing/unpublished slug). | blocker | Seed neutral values (`"Dhaka Bypass Expressway"`) into `seo.site_title`; make the code default neutral too. |
| 2.4 | `lib/seo/settings.js:69-70` | `siteDescription: 'Official website of the Dhaka Bypass Expressway project, spanning 48km and connecting major national highways around Dhaka.'` — "48km" is a fact; `seo.site_description` is `{}` in the DB so this is the live `<meta name="description">` wherever a route emits none of its own (the 404 document). | blocker | As 2.3; if a length must appear, compose it from `corridor.published_length_km`. |
| 2.5 | `lib/corridor/road-references.js:3-11` | `N1: {name:'Dhaka–Chattogram Highway', source:'https://rhd.gov.bd/…'}` … seven highway names and RHD URLs. Rendered to visitors: `lib/corridor/view.js:124-125` copies `reference.name`/`reference.source` into `geography.roads`, and `CorridorExplorer.jsx:284-287` shows them as the road panel's title and "Road reference" link. English only, no admin. | blocker | A `corridor_roads` record (ref, names JSON, source URL) under `/admin/corridor`, or at minimum a `site_settings` JSON edited on the corridor screen; the view reads the record, the code table becomes the outage fallback. |
| 2.6 | `lib/corridor/data/map-context.json` (imported at `lib/corridor/view.js:15`) | 103 OSM road highlights with English `name`s (`Tongi Kaliganj Highway`, `Dhaka-Mymensingh`) and 103 junctions; built by `scripts/build-corridor-context.mjs`. Rendered as road names, badges and tooltips on the map (`CorridorMap.jsx:29,37`, `CorridorExplorer.jsx:284`). Not localised, not editable; a rebuild changes what the visitor sees with no admin action. | major | Import the highlight/junction set into a record table on build (like `corridor_geometry`) so names can be corrected/translated in the admin; keep the JSON as fallback. |
| 2.7 | `lib/i18n/ui.js:106` (and bn `:237`, zh `:362`) | `mapAltText: 'Map of the Dhaka Bypass Expressway corridor from Naojor to Madanpur, …'` — the map's accessible name (`lib/corridor/view.js:200`) hardcodes the two terminus names that are `interchanges`/`corridor_waypoints` records (`Naojor (corridor start)`, `Madanpur (corridor end)`). Renaming a terminus in `/admin/corridor/interchanges` leaves the screen-reader name wrong. | major | Make the string a template (`{from}`/`{to}`) filled from the terminal waypoint names, as `filterCount` already does with `{n}`. |
| 2.8 | `lib/i18n/ui.js:105` | `mapIntro: 'The alignment from Naojor to Madanpur, …'` — same fact; the key has NO call site (see 4.6) but is offered for editing. | minor | Delete the key (the intro is the corridor-map block's `intro` field now). |
| 2.9 | `lib/i18n/ui.js:149` | `travelTollIntro: '… Rates for the full 48 km corridor have not yet been published.'` — a fact and a status claim; no call site. (The live copy of this sentence is block 326's lede, see 5.11.) | minor | Delete the key. |
| 2.10 | `lib/i18n/ui.js:103` | `galleryResolutionNote: '… the largest is 1024 pixels wide …'` — a fact about the media set; no call site. | minor | Delete the key. |
| 2.11 | `lib/i18n/map-ui.js:19` | `title:'Dhaka Bypass'` — the map panel's brand line is a ui string, not `seo.org_short_name`; `highlight:'Up to 2 km along each approach'` restates the `2000` m constant at `scripts/build-corridor-context.mjs:118` in prose. | minor | Read the org short name from settings; derive "2 km" from the build constant or drop the number. |
| 2.12 | `lib/seo/organization.js:79-80` and `lib/seo/settings.js:75-76` | `ORG_NAME`/`ORG_SHORT_NAME` literals duplicated in two modules as defaults. The DB has both settings populated, so not live; but two default copies drift. | minor | `SEO_DEFAULTS` should import the constants. |
| 2.13 | `components/blocks/TrafficStatusBlock.jsx:24` | `const DHAKA_UTC_OFFSET_MINUTES = 360;` — a timezone as a number. | minor | `timeZone: 'Asia/Dhaka'` in `Intl.DateTimeFormat`. |
| 2.14 | `components/chrome/SiteHeaderV2.jsx:64` | `width={43} height={34}` — the intrinsic size of DBEDC's default mark, kept when an operator sets `brand.header_logo` to any other file. | minor | Resolve dimensions through `lib/seo/identity.js` `resolveLogo()` (already measures files) or the `media` row. |
| 2.15 | `lib/corridor/toll-formula.js:80-89` (`DBEDC_2025`, `CLASS_BASIS`), `lib/institutional/pages.js` | Per-km rates, `corridorKm: 48.4`, uplift 115 %, access charge 50, and the full text of ten institutional pages live in code as SEED SOURCES (`db/sql/12-toll-od-matrix.sql`, `scripts/seed-institutional.mjs`). Not rendered at runtime; all 270 `toll_od_rates` rows derive from the formula and 0 carry an S.R.O. citation. `--replace` on the seed script would overwrite operator edits. | minor | Keep as history; guard the seed scripts against running on a database that has operator edits (`revisions` non-empty). |

Also noted: `DBEDC` appears in 15 English `ui.js` strings (`contactHeading`, `formSentBody`, `newsletterNote`, `errorBody`, …) rather than being composed from `seo.org_short_name`; renaming the organisation in `/admin/settings` leaves them. All are editable strings, so this is a consistency note, not a finding row.

## 3. Admin ↔ public consistency

Method: for each of the 41 types in `lib/blocks/index.js`, every `fields[].name` and `itemFields[].name` in `lib/blocks/types/<type>.js` was checked against the `data.*` reads in `components/blocks/<Component>.jsx` and its helper in `lib/blocks/*.js` / `lib/corridor/toll-matrix.js`.

| # | file:line | finding | severity | suggested fix |
|---|---|---|---|---|
| 3.1 | `lib/blocks/types/card-grid.js:24` vs `components/blocks/CardGridBlock.jsx:9-14` | The type declares `items[].image` ("Image" picker, rendered by the admin per `tests/unit/admin-block-fields.test.jsx`); the renderer reads only `meta`, `title`, `body`. An operator uploads a card image and nothing appears. 17 card-grid blocks are in use. | major | Render the image through `getMediaByPath` + `SiteImage` as `FigureGridBlock` does, or remove the field. |
| 3.2 | `app/admin/(dash)/settings/page.jsx:126-135`, `lib/settings.js` (`SOCIAL_KEYS`, `getContactDetails().social`) | Four social-link fields (facebook, youtube, linkedin, x) are saved and read, but no chrome or block renders `details.social` (`ContactFormBlock.jsx` ignores it; `SiteFooterV2.jsx` never reads it; `organizationJsonLd` has no `sameAs`, `organization.js:56`). | major | Render in the footer bottom bar and as `sameAs` in the Organization JSON-LD, or remove the fields. |
| 3.3 | `components/blocks/BlockRenderer.jsx:60` | See 1.2: the fallback flag from `resolveTranslation` is discarded; preview and public behave identically (both without a notice). | major | As 1.2. |
| 3.4 | `components/blocks/NewsListBlock.jsx:11`, `GalleryGridBlock.jsx:9` | `limit` is clamped to 1..200 silently; the admin number field accepts 0 or 5000 without a message. | minor | Declare `min`/`max` on the field and validate in `validateBlockData`. |
| 3.5 | `components/blocks/FigureGridBlock.jsx:12-16` | A photograph row with a caption but no image (or an image path with no `media` row) is dropped without trace; the editor shows it as a full row. | minor | Show the editor a warning, or render the caption-only row. |
| 3.6 | `lib/content/pages.js:32` vs `db/sql/02-seed.sql:213` and `pages.js:239-245` | `blocks.status` exists (seeded `'published'`, copied by `duplicatePage`) but `getPageBlocks` never selects it and nothing reads it; publish state is per `block_translations.status`. A column that looks like a switch and is not one. | minor | Drop the column or make it authoritative. |
| 3.7 | `app/admin/(dash)/corridor/tolls/page.jsx:33` vs `TollTableBlock.jsx`, `TollPreviewBlock.jsx` | `toll_rates.effective_from` is required in the admin and `payment_methods` is stored; neither is rendered by any block, so "in force since" never reaches a visitor. | minor | Add an optional "effective from" column/line to `toll-table`. |
| 3.8 | `app/[locale]/preview/[id]/page.jsx`, `components/admin/PreviewPane.jsx`, `lib/content/preview.js` | **Consistent.** The preview renders `BlockRenderer` inside `app/[locale]/layout.jsx` (real header/footer/advisory/tokens); the only transform is `withDraftTranslations`. `presentationClasses(block.settings)` is computed in the renderer, and both routes get `settings` from `getPageBlocks` (`pages.js:46`), so background/spacing/width/align are identical in preview and public. Covered by `preview-route.test.jsx`, `preview-blocks.test.jsx`, `block-presentation.test.js`. | — | none. |
| 3.9 | all other 39 types | **Consistent.** Every declared field is read by its renderer or helper (`selectRates`, `tollCitation`, `selectInterchanges`, `visibleColumns`, `selectSections`, `buildMatrix`, `wantsFilter`, `pickRates`, `resolveVideo`, `fieldPlan`); no renderer reads a `data.*` key its type does not declare. | — | none. |

## 4. Operator dead ends

| # | file:line / screen | finding | severity | suggested fix |
|---|---|---|---|---|
| 4.1 | national emergency number — `SiteFooterV2.jsx:102`, `EmergencyStripBlock.jsx:33` | Edited **nowhere**; code literal `999` (see 2.1/2.2). | major | Setting on `/admin/settings`. |
| 4.2 | footer legal links — `SiteFooterV2.jsx:116-118` | Targets `localeHref('privacy')`, `'terms'`, `'accessibility'` are code; labels are `ui.footerPrivacy/…` (editable). Deliberately outside the `footer` menu ("must not quietly disappear"), so renaming or unpublishing one of those three pages leaves a footer link that 404s; an operator cannot add a fourth (e.g. Cookies). Edited **nowhere**. | major | A `legal` menu slug in `lib/menus/slugs.js` with the three built-ins as its outage fallback — the same override-not-replace pattern the other three menus use. |
| 4.3 | header Contact CTA — `SiteHeaderV2.jsx:78,94` | `href={\`/${locale}/contact\`}` with label `t(locale,'navContact')`; not an item of the `main` menu, so an operator who builds the main menu still gets a hard-wired button after it. Edited **nowhere** (label at `/admin/translations`). | major | Make the CTA the last `main` menu item flagged `is_cta`, or a `brand.cta_href` setting. |
| 4.4 | `corridor.prohibited_vehicles` — read by `lib/settings.js:39` → `ProhibitedVehiclesBlock.jsx` | The three-locale list of banned vehicle classes is a `site_settings` JSON seeded by `db/sql/02-seed.sql:33`. No form field anywhere under `app/admin/**` writes it (grep: only the seed and the block type's comment mention it). Edited **nowhere**. | major | Three textareas (one per locale, one line per class) on `/admin/corridor`. |
| 4.5 | `corridor.published_length_km` — `lib/settings.js:56` → `ProgressBarBlock.jsx` / `ProgressBar.jsx:39` | The "/ 48 km" denominator on every progress bar; seeded (`02-seed.sql:34`), no admin field. Edited **nowhere**. | major | Number field on `/admin/corridor` beside the "illustrative" checkbox. |
| 4.6 | `/admin/translations` — `lib/i18n/catalogue.js` lists every key of `UI.en` | 31 keys have no call site anywhere in `app/`, `components/`, `lib/` (checked by scanning for the quoted key): `legacyDataNotice, contactHeading, contactIntro, contactOtherHeading, contactOtherBody, newsHeading, newsIntro, galleryHeading, galleryIntro, galleryResolutionNote, mapIntro, mapDistribution, mapMonthlyFlowNote, mapLatestMonth, mapSections, mapCorridorInfo, mapMonthlyFlow, mapMonthChange, mapOverall, mapWorst, mapPeak, travelStatusIntro, travelTollIntro, travelRouteIntro, travelFacilitiesIntro, travelRulesIntro, routeCaption, rulesEmpty, homeCorridorHeading, navImpact, seeAllTolls, seeRoute`. An operator edits any of them and nothing on the site changes. (`requestKind_*` and `traffic_*` are reached dynamically and are fine.) | major | Delete them from `ui.js`; add a test that every key is referenced (allowing documented dynamic prefixes). |
| 4.7 | sitemap "essential" paths — `lib/seo/routes.js:95-105`, `lib/seo/sitemap.js:148` | `/contact, /gallery, /news, /travel/*` (9 paths) are pushed to `sitemap.xml` unconditionally AFTER the published pages, so unpublishing or deleting `/gallery` in `/admin/pages-v2` still advertises `/en/gallery`, `/bn/gallery`, `/zh/gallery` to crawlers (the only escape is a `route_meta` noindex row). Edited **nowhere** as a list. | major | Only fall back to the list when the pages read FAILED, not when it succeeded with fewer rows. |
| 4.8 | "shown in English" notice for block documents | News: `ui.newsInEnglish` ✓. Block pages: no notice exists to edit (1.2). | major | As 1.2. |
| 4.9 | social links — `/admin/settings` (Contact → Social) | Fields exist and save; rendered **nowhere** (3.2). | major | As 3.2. |
| 4.10 | map road names / sources — `lib/corridor/road-references.js`, `map-context.json` | Edited **nowhere** (2.5/2.6). | major | As 2.5/2.6. |
| 4.11 | theme toggle labels — `ThemeToggle.jsx:6` | Edited **nowhere** (1.1). | major | As 1.1. |
| 4.12 | site title / description — `/admin/settings` (Search) | Editable ✓, but both rows are `{}` in the DB so the code defaults with claims are what ships (2.3/2.4). | minor | Seed neutral rows; the screen already works. |
| 4.13 | `interchanges.connects_to` — `/admin/corridor/interchanges` | A single-language `varchar`, rendered in the "Connects" column on every locale (`InterchangeTableBlock.jsx:53`) while `names` is a per-locale JSON. All rows are empty today, so nothing is visibly wrong yet. | minor | Make it JSON per locale like `names`. |
| 4.14 | menus — `/admin/menus` | `main` has no row in `menus`; `footer` and `travel` exist with 0 items. So today the header, footer columns and travel sub-nav all render the CODE lists in `lib/menus/builtin.js`, by design (override-not-replace). Editable; the "start from the built-in links" seed exists. Noted so nobody mistakes the current chrome for a menu. | minor | none; consider seeding the three menus so the operator starts from visible rows. |
| 4.15 | brand tagline, 404 document, newsletter privacy line, toll-calculator labels, gallery per-page count, map legend toggle, robots mode/disallow, per-route noindex | All have a home: `ui.brandTagline` (`/admin/translations`); `pages.not-found` (`/admin/pages-v2`); `newsletter-form.note` or `ui.newsletterNote`; `toll-calculator.*Label` with `ui` fallbacks; `gallery-grid.limit`; `corridor-map.showLegend` + `ui.traffic_*`/`mapKey*`; `/admin/settings` Crawling; `route_meta` (`/admin/seo`). | — | none. |
| 4.16 | header aria-labels — `SiteHeaderV2.jsx:72,88` | Edited **nowhere** (1.3). | minor | As 1.3. |

## 5. Records vs blocks

### 5a. Block TYPES that can hold a copy of a record

| # | file:line | finding | severity | suggested fix |
|---|---|---|---|---|
| 5.1 | `lib/blocks/types/toll-table.js:54-57` (`sroNumber`, `sroDate`, `sroLink`, `revisionMechanism`) | Gazette provenance is authored per BLOCK, while the same citation is a per-RECORD column set on `toll_od_rates.sro_number/sro_date/sro_link` (`db/sql/12-toll-od-matrix.sql`) that also drives `is_provisional` for the matrix and calculator. `toll_rates` has no citation columns at all, so the schedule table and the matrix can cite different notifications for the same rates. | major | Add `sro_*` to `toll_rates` (or a `toll_notifications` record joined from both tables); the block keeps only presentation. |
| 5.2 | `lib/blocks/types/stat-row.js`, `stat-dashboard.js` — free-text `value`/`unit`/`label` | The only way to show corridor length, open length, measured length, number of plazas or vehicle classes is to TYPE the number. There is no record-bound statistic. This is the type gap behind 5.9. | major | A `live-stat` item kind (`source: published_length_km \| open_length_km \| measured_length_km \| toll_plaza_count \| vehicle_class_count`) resolved from records at render, alongside free text for genuinely authored figures. |
| 5.3 | `lib/blocks/types/data-table.js` — free-text `rows` | Nothing stops a toll schedule or an interchange list being retyped into it (and it has been: 5.6). | major | Keep the type, add a content lint (see §6) that refuses a data-table row containing a taka amount or a `K\d+\+\d{3}` chainage while a live block exists for that fact. |
| 5.4 | `lib/blocks/types/request-form.js:31` (`slaDays`) with `lib/requests/policy.js` | The response deadline per request kind is a code default (grievance 30, toll dispute 15, breakdown 1, lost & found 7, general 14) that every block can override; the deadline shown to a citizen is therefore per-block, not a record. | minor | A `requests.sla_days` setting per kind on `/admin/requests`; the block field becomes an explicit exception. |
| 5.5 | `emergency-strip`, `contact-form`, `toll-preview`, `toll-table`, `toll-matrix`, `toll-calculator`, `interchange-table`, `traffic-status`, `facility-list`, `progress-bar`, `corridor-strip`, `corridor-map`, `prohibited-vehicles`, `news-list`, `gallery-grid`, `section-subnav`, `sitemap-list` | **Correct.** All read records/menus/settings and store presentation only (except 5.1 and the `999` literal). | — | none. |

### 5b. Live CONTENT in the database that duplicates (and already contradicts) records

These are block rows, not code, but they are exactly what the client's rule forbids, and they are what a visitor sees today. Block ids from the local DB.

| # | block / page | finding | severity | suggested fix |
|---|---|---|---|---|
| 5.6 | block **425** `data-table` on `travel/toll` | A hand-typed copy of the toll schedule: rows like `["Large truck, 3+ axles (trailer), 15–25 t", "1,600", "(33.06 × km + 50) × 115%", "740"]` … nine classes with full-corridor amount, formula and 18 km amount — on the SAME page as `toll-table` 327, `toll-matrix` 441 and `toll-calculator` 440, which read `toll_rates`/`toll_od_rates`. A rate change at `/admin/corridor/tolls` updates three blocks and not this one. | blocker | Delete the block, or replace it with `toll-matrix` plus a `rich-text` note about the formula (which is derivation, not a rate). |
| 5.7 | block **422** `card-grid` on `travel/route` | Interchanges retyped: `"Joydebpur" (Northern terminus)`, `"Vogra … K3+900 marks the beginning of the partial toll collection point"`, `"Kanchan / Purbachal … at the K22+00 marker"`. The `interchanges` records on the same page (`interchange-table` 332) say `Naojor (corridor start)` at K0+000, `Vogra Toll Plaza (RHS)` at **K3+218**, `Purbachal Toll Plaza` at **K24+522**. The page contradicts itself. | blocker | Delete the cards; move any genuinely descriptive prose into `interchanges` (add a per-locale `description` column rendered by `interchange-table`). |
| 5.8 | block **424** `rich-text` on `travel/status` | `"An 18 km section from Vogra (Gazipur) to Kanchan (K3+900 to K22+00) opened …"` — the open segment record is `segments` 3218→21218 (K3+218 to K21+218), rendered by `progress-bar` 322 and `corridor-strip` 323 on the same page. | blocker | Keep the history sentence but strip the chainages/length, or express them as `live-stat` (5.2). |
| 5.9 | blocks **124** (`home`), **130** (`about`), **140** (`project`) `stat-row` | `Corridor length 48 km` (×3), `Open to traffic 18 km` (×3), `Measured on the road network 47.611 km` — copies of `corridor.published_length_km`, `segments` and `interchanges.chainage_m` (Madanpur 47611). | blocker | `live-stat` (5.2). |
| 5.10 | block **423** `card-grid` on `travel/facilities` | `"Two service areas, one in each direction …"`, `"Five toll plazas …"` — `interchanges` has **0** `service_area` rows (the `facility-list` block 338 directly below therefore prints the empty message) and 9 `toll_plaza` rows. | blocker | Delete or convert to record-driven counts. |
| 5.11 | block **326** `page-header` on `travel/toll` | Lede `"… Rates for the full 48 km corridor have not yet been published."` — the length duplicates `published_length_km`; the status claim is now FALSE on the same page, where `toll-matrix` 441 shows 270 origin-destination fares for the whole corridor (provisional). | blocker | Reword without the number and the claim. |
| 5.12 | block **121** `toll-preview` on `home` (`intro`) | `"Motorcycles and three-wheelers may not use the expressway."` duplicates `corridor.prohibited_vehicles` (rendered by `prohibited-vehicles` 328). | major | Drop the sentence; place a `prohibited-vehicles` block. |
| 5.13 | `page_translations.seo_description` for `home` (en/bn/zh) | `"… open and tolling on 18 kilometres …"` / `"… Vogra থেকে Purbachal … ১৮ ক…"` — the open length again. | minor | Reword or template from records. |
| 5.14 | block **142** `card-grid` on `project`, block **362** `rich-text` on `safety` | `"18 km carrying traffic"`; `"Emergency call points are placed every 2 km"` — the first duplicates `segments`; the second is an unverified operational claim with no record behind it. | major | `live-stat`; source the call-point spacing or mark it pending. |

## 6. Tests that would catch regression

### Existing drift guards (verified by reading the test names/bodies)

| # | test | what it guards |
|---|---|---|
| 6.1 | `tests/unit/seo-routes.test.js` | Walks `app/[locale]/` and fails if any content `page.jsx` appears beyond the three declared exceptions — the structural "no hardcoded pages" rule. |
| 6.2 | `tests/unit/blocks-registry-coverage.test.js` | Every `components/blocks/*Block.jsx` is registered exactly once; every type has label, fields, Component. |
| 6.3 | `tests/unit/admin-block-fields.test.jsx`, `block-form.test.js`, `block-list-fields.test.js` | The admin renders a control per declared field/sub-field (image picker, repeater rows, select). Admin side only — it would NOT catch 3.1. |
| 6.4 | `tests/unit/preview-route.test.jsx`, `preview-blocks.test.jsx` | Preview is session-gated, uses uncached readers, promotes drafts, renders through the same renderer. Covers 3.8. |
| 6.5 | `tests/unit/block-presentation.test.js` | Presentation enums, defaults and class emission. |
| 6.6 | `tests/unit/ui-strings.test.js`, `menus-builtin.test.js` | Every `ui.js` key exists in all three locales; every built-in menu key resolves. |
| 6.7 | `tests/unit/blocks-corridor-live.test.js`, `blocks-toll-*.test.*`, `corridor-strip-data.test.js` | Live blocks select/sort from records and declare every sort they offer. |
| 6.8 | `tests/unit/institutional-pages.test.js` | The SEED file states no unconfirmed currency amount and marks gaps in all locales — the seed, not the database. |
| 6.9 | `tests/unit/seo-settings.test.js` — "returns the code defaults when nothing is stored" | **Locks 2.3/2.4 in**: it asserts the "48km" description is what an empty setting yields. |
| 6.10 | `tests/unit/seo-sitemap.test.js` — "keeps every essential content route in the sitemap when the database gave nothing" | **Locks 4.7 in** for the outage case; there is no case for "essential page exists but is unpublished". |

### Missing guards (none of these exist today)

| # | proposed test | catches |
|---|---|---|
| 6.11 | Type ↔ renderer field parity: for each registered type, every `fields[].name`/`itemFields[].name` must appear as `data.<name>` / `.<name>` in the Component source (or a helper it imports), and every `data.<key>` read must be declared. | 3.1 (card-grid image) and any future drift. |
| 6.12 | Literal lint over `components/chrome`, `components/blocks`, `components/corridor`, `app/[locale]`: fail on JSX text nodes or string props (`aria-label`, `title`, non-empty `alt`) that are not `t()`/`ui.`/props; fail on `/\b999\b/`, `/\d+ ?km/`, `/K\d+\+\d{3}/`, terminus names. | 1.1, 1.3-1.7, 2.1, 2.2, 2.13. |
| 6.13 | Unused `ui.js` key guard (each key referenced in `app/`/`components/`/`lib/`, with an allow-list of dynamic prefixes `requestKind_`, `traffic_`, `nav`). | 4.6, 2.8-2.10. |
| 6.14 | Settings ↔ admin parity: every `site_settings` key read in `lib/**` (`getSetting('…')`, `*_KEYS`) has an `<input name=…>` in `app/admin/(dash)/settings` or `corridor/**`; every key WRITTEN by an admin action is read by a renderer. | 4.4, 4.5, 3.2/4.9. |
| 6.15 | Sitemap: an ESSENTIAL path whose `pages` row is unpublished is omitted when the pages read succeeded. | 4.7. |
| 6.16 | `BlockRenderer` fallback: a `/bn` render of an English-only block emits the fallback notice and keeps `bn` chrome. | 1.2/3.3. |
| 6.17 | SEO defaults are claim-free: `SEO_DEFAULTS.siteTitle/siteDescription` match `/^Dhaka Bypass Expressway/` and contain no digit; and a `tests/db` check that `seo.site_title`/`seo.site_description` rows exist per locale. | 2.3, 2.4. |
| 6.18 | Records-vs-content lint (`tests/db` or a `scripts/lint-content.mjs` run in CI against the SQL seeds and, optionally, production): no `data-table`, `stat-row`, `card-grid`, `rich-text`, `page-header` translation contains `৳`, a taka amount adjacent to a vehicle class, a chainage `K\d+\+\d{3}`, or the value of `corridor.published_length_km`, on any page that also carries a live block. | 5.6-5.14. |
| 6.19 | Map road references come from a record: `lib/corridor/view.js` must not import a static name table; snapshot that the road panel names resolve per locale. | 2.5, 2.6. |
| 6.20 | Footer/header chrome targets come from menus or settings: no `href` literal under `components/chrome` other than `#main`, `/${locale}` and `tel:`/`mailto:` composed from settings. | 4.2, 4.3. |

---

### Method notes

* Dev server at `:3000` was not running during the audit (`curl` returned no response), so rendering claims were verified by reading the components and the metadata resolver (`next@15.5.25` `resolve-metadata.js:207` — an empty page description is assigned as-is, so only routes that return `{}` from `generateMetadata` inherit the root default).
* Database facts came from read-only queries against the local `.env.local` database; no rows were written. Temporary query scripts were kept in the session scratchpad, outside the project.
* No Playwright scripts or screenshots were created inside the project.

---

## Resolution — 2026-09-13

Every finding above was acted on in commits `45c3e68`, `51556e4`, `26ef8b3` and the drift-guard commit that follows them. Summary by section:

**1. Hardcoded public text** — theme labels, header nav labels, units (km, m), the pull-quote source link and the map's road-title fallback read `ui_strings`/map strings (1.1, 1.3–1.7). A block shown in English on a translated page keeps the page's locale for its chrome and prints `ui.blockInEnglish` (1.2/3.3). The sitemap block never humanises a slug (1.8).

**2. Hardcoded facts** — `999` is `contact.national_emergency_phone` on `/admin/settings`, rendered by one component in the footer and the emergency strip (2.1/2.2). SEO defaults carry no claim or figure, and 31 seeds neutral per-locale `seo.site_title`/`seo.site_description` (2.3/2.4). Road names and references are the `corridor_roads` record at `/admin/corridor/roads`, per language, seeded from the RHD register; `lib/corridor/road-references.js` is deleted (2.5/2.6). The map's accessible name is a `{from}`/`{to}` template filled from the terminal waypoints (2.7). The 32 dead ui keys are deleted (2.8–2.10, 4.6). The map title plate's road number is `corridor.road_code` (was `N105` in code). Organisation defaults have one copy (2.12). Measured times format in `Asia/Dhaka` by name (2.13). The header logo's width is measured from the configured file (2.14). `seed-institutional.mjs --replace` refuses on a database with saved revisions unless `--discard-admin-edits` is passed (2.15).
*Left as is:* `map.title` ("Dhaka Bypass") and `map.highlight` ("Up to 2 km…") are already editable strings (2.11); the 15 English strings naming DBEDC are editable strings (consistency note).

**3. Admin ↔ public** — card-grid renders `items[].image` (3.1); social links render in the footer and as Organization `sameAs` (3.2); `limit` fields declare `min`/`max`, enforced by `validateBlockData` and the number input (3.4); the toll table prints "In force since" from `toll_rates.effective_from`, switchable per block (3.7).
*Left as is:* a figure-grid row with no image is still skipped (3.5 — the preview shows the gap); `blocks.status` is still unused (3.6 — dropping a column needs its own migration and an audit of every seed that writes it).

**4. Operator dead ends** — the footer policy links are the `legal` menu and the header button is the `cta` menu, both override-not-replace with built-in fallbacks (4.2/4.3); `/admin/corridor` edits published length, road number and prohibited vehicles per language (4.4/4.5); the sitemap lists essential pages only when the pages read failed (4.7); `connects_to` is per language (4.13). Built-in menu seeds now store the authored (localised) href form; 31 rewrites any seeded literal ones.

**5. Records vs blocks** — the toll citation is on `toll_rates` (`sro_number`, `sro_date`, `sro_link`), entered at `/admin/corridor/tolls`; the block keeps only the revision prose (5.1). A statistic item can take its value from a record — published, measured and open length, tolled class count, interchange count (5.2/5.9). Response deadlines per request kind are `requests.sla_days` on `/admin/requests` (5.4). Migration 31 deletes blocks 425, 422 and 423 (5.6, 5.7, 5.10), strips lengths and chainages from 424, 326, 121, 142, 362, the home/about/project/safety prose, the home hero and the search descriptions (5.8, 5.11–5.14), and drops the contradictory "48.07 km" and "6 toll plazas" specification cards.

**6. Tests** — added: `ui-keys-referenced` (6.13), `cms-drift-guards` (6.11 field parity both ways, 6.12 literal wording and facts, 6.14 settings ↔ admin, 6.16 fallback notice, 6.17 claim-free SEO defaults, 6.19 road names as a record, 6.20 chrome hrefs), the sitemap outage-only case (6.15), and in `tests/db/fresh-import` the 31 schema/settings guarantees plus a content lint for copied lengths and chainages (6.18). The two tests that locked the old behaviour in were updated.
